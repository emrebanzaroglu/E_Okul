@model IEnumerable<E_Okul.Dto.StudentDTO>

@{
    ViewData["Title"] = "List";
}

<h1>Öğrenci Listesi</h1>


<a asp-action="Add" class="btn btn-primary">Yeni öğrenci ekle</a>

<table class="table">
    <thead>
        <tr>
            <th>Resim</th>
            <th>Ad Soyad</th>
            <th>Cinsiyet</th>
            <th>Tc No</th>
            <th>Okul No</th>
            <th>İşlem</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td><img src="@item.Picture" height="100px" width="100px" /></td>
                <td>@item.FullName</td>
                <td>@item.Gender</td>
                <td>@item.TCNo</td>
                <td>@item.SchoolNo</td>
                <td>
                    <a asp-controller="Student" asp-action="Update" asp-route-id="@item.Id" class="btn btn-success">Güncelle</a>
                    <a asp-controller="Student" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Sil</a>
                    <a asp-controller="Student" asp-action="Detail" asp-route-id="@item.Id" class="btn btn-info">Detay</a>
                </td>
            </tr>
        }
    </tbody>
</table>
